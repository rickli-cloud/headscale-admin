package utils

// import (
// 	"io/fs"
// 	"log"
// 	"os"
// )

func IsSocket(path string) bool {
	// fileInfo, err := os.Stat(path)
	// if err != nil { 
	// 	log.Fatal(err.Error())
	// }
	// isSocket := fileInfo.Mode().Type() == fs.ModeSocket
	// return isSocket
	return true
}